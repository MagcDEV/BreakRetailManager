@page "/accounts/employee/{AccountId:guid}"
@inject NavigationManager Navigation

@code {
    [Parameter] public Guid AccountId { get; set; }

    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/expenses", replace: true);
    }
}
